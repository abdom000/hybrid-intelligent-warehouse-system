# Shelf Image Placeholders

The image dataset is not included in the initial mock-data package.

Use `manifest.json` as the agreed file naming and label contract. When real or synthetic shelf images are added, update each manifest entry's `available` field to `true`.

Expected classes:

- `empty`
- `low_stock`
- `normal`
- `full`
- `unknown`
